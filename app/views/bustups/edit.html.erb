<div class="contents row">
  <div class="container">
    <h3>編集する</h3>
    <div class="contents row">
      <div class="container">
        <%= form_with(model: @bustup, local: true) do |form| %>
          <%= form.file_field :image, id:"bustup_image" %>
          <%= form.text_field :title, placeholder: "タイトル" %>
          <%= form.submit "SEND" %>
        <% end %>
     </div>
   </div>
  </div>
</div>